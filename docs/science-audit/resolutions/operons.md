# operons Phase B resolution

五项原始问题均已修复；Phase A 记录未改动。未使用浏览器，最终画面验收由 root 完成。

| Issue | 修复 | 回归证据 |
| --- | --- | --- |
| operons-01 | 每个活动 lac RNAP 独立打开本地 DNA；一个终止后不关闭其他转录泡。 | 读取骨架与碱基实例坐标，覆盖全部四条件与 .80/.88/.92 等时点；恢复旧逻辑后测试失败。 |
| operons-02 | 低 Trp 条件明确为“严重匮乏、带 Trp 的 tRNA 不足”，区分轻度游离 Trp 降低。 | 双语条件加四种情况下真实氨基酸标记、RNA 分支和延伸几何；恢复模糊标签后失败。 |
| operons-03 | Pbs2-P → Hog1-P → 经孔入核 → 核内 RNA；去磷酸化后出核。 | 四条件各 201 帧，检查实际位置和磷酸标记；恢复提前入核后在 .435 失败。 |
| operons-04 | 操作明确为 Hog1 激活位点“不可磷酸化”，与催化抑制区分。 | 双语操作语义、真实磷酸标记与核位置；恢复泛称 Inhibited 后失败。 |
| operons-05 | 实际膜环开孔、径向孔道路径、孔径内可容纳的完整运输对象；进出核共用孔。 | 从索引膜面推导真实开口，检查运输对象全部可见顶点和实际环形孔径；恢复旧直线路径后在 .55 失败。 |

局部命令全部通过：`smoke.mjs`、`yeastSmoke.mjs`、`science.test.mjs`、`science.mutations.test.mjs`（均在 `src/processes/modules/operons/`）。后两者还覆盖 16 种条件、有限顶点、稳定资源、反向 seek、四个模型的 esbuild，以及五个只存在内存中的缺陷复发实验。

GAL 没有针对性改动；共享 helper 的默认路径纳入回归。旧参数值 `low` 和 `inhibited` 保留以兼容既有状态，界面文字和情景语义已经明确。

新增内嵌依据：[Yanofsky 1984](https://pubmed.ncbi.nlm.nih.gov/6233264/) 支持两种 Trp 响应的不同阈值；[Ferrigno 1998](https://pubmed.ncbi.nlm.nih.gov/9755161/) 支持 Hog1 磷酸化与核转运的关系。二者均于 Phase A 实际查阅原始摘要。

模型仍是定性分子示意：不模拟真实反应时间、原子轨迹、核转运辅助蛋白或连续 Trp 饥饿阈值。测试证明所列回归约束，不能替代全部科学事实审核或最终视觉验收。每项具体文件、命令和限制见同名 JSON。

## Peer correction: peer-operons-01

独立复核指出旧展开公式在泡肩把两条链向固定 ±y 方向插值，造成管面穿插。现改为共同中心线与正交框架内的相反径向路径，半径始终为正；积分非负局部扭转密度，让泡内逐步解旋，关闭后回到原双螺旋。每条链的材质、碱基连接及连续线段身份保留。

新增 `duplexGeometry.test.mjs` 覆盖全部四模型的 16 种条件，每条件 1002 时点（包含精确故障时刻 .7385）。从实际实例矩阵重建线段和管半径，比较不同线段、不同参数处的精确有限段距离，以保守扫掠体约束真实管面间距；同时检查链段连续。测试通过。`science.mutations.test.mjs` 增加恢复旧泡肩公式的反例，要求该回归实际捕获旧穿插。

该展开允许下游示意性的扭转松弛，不是端点固定的力矩模拟；时间采样与管面间距约束也不是原子结构证明。已请 `/root/p_rna` 独立重跑原探针及跨参数段检查，最终复核结论待其记录。
