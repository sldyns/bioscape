# Signals Phase B 修复闭环

原始 8/8 issueId 已修复；追加修复 peer-signals-01。仅修改 signals 本组；Phase A 和独立验收报告未改，未操作浏览器。根代理仍需渲染复核。

## peer-signals-01 · fixed（追加修复，独立复验待执行）

独立复核发现：核膜腰部缩窄时，未随形的染色质会穿过核膜，虽然仍位于质膜内。现将核膜和全部 DNA／linker／组蛋白芯顶点先变换到共同核内坐标，应用同一 waist 变换后再还原各自局部坐标。每帧从初始顶点计算，保留所有网格、顶点和可见性。

新增回归对 competent 和 impaired 各检查 12 帧，包括 .835 和质膜分裂进度 .866254 两侧。全部 **1,556,064 个 DNA 表面顶点**对实际核膜三角面的包含判定通过；**144 对完整 DNA／核膜网格**直接三角面相交测试均不相交。原核膜在质膜内、8项几何科学回归、可变几何字节往返跳转、有限顶点和资源稳定性检查保留并通过。本组 smoke 四模型通过。

修改：`differentiationProcess.js`、`science.test.mjs`。已联系 p_signals，请根代理触发独立复验。该共同形变是几何示意，并非核力学模拟；有限采样不是所有连续时刻的证明。

## signals-01 · fixed

重新布置 SOS 催化面与膜锚定 Ras，使 Grb2 仍贴近受体尾；显式 GDP 离开和 GTP 到位，仅活性受体条件发生交换。

验证：实际 SOS 催化球表面点进入 Ras 椭球接触区后才显示交换，检查 p=.415/.42/.43/.6/1；noLigand/kinaseInactive 保留GDP。

边界：蛋白界面是机制示意，不是1BKD原子坐标轨迹。

## signals-02 · fixed

移除 Ras 的 kinaseFold 子结构，保留单一小GTP酶域、核苷酸位置和两条switch区示意。

验证：Ras对象不含激酶fold子组；小GTP酶与激酶使用不同几何身份。

边界：未在src/public中找到可复用的1BKD结构资产，采用保守的非原子外形。

## signals-03 · fixed

共享 chromatinFiber 改为芯外左手超螺旋包绕，沿包绕中心线生成右手DNA双链，使用连续linker连接；组蛋白芯不再放在DNA双螺旋轴内。

验证：对四模型所有DNA管面顶点逐芯做实体排斥测试：没有顶点进入组蛋白圆柱实体；每fiber均有两条连续链。

边界：核小体是带双链/芯/连接段的结构示意，不是1AOI原子重建；核小体数量不代表基因组计数。

## signals-04 · fixed

改用单一连续等值膜面：重叠叶之间无内部重复膜，外移产生膜颈并最终分离；五个预先存在的染色质货物对象沿轨迹分配，不克隆新小体DNA。保留母体残留并收拢线粒体以避免缩小后穿膜。

验证：抽样全过程对膜三角网格验证每条边有两个邻面；初态1个连通体、终态母体残留+5个小体共6个；同一DNA货物对象始终被实际三角面包围。 noStress终态仍为1个闭合膜体；资源对象稳定。

边界：膜连续变形是几何插值，非细胞膜力学模拟；不展示巨噬清除。

## signals-05 · fixed

将完整内边界叠加悬空嵴条改成单条闭合内折轮廓及其连续膜壁；每个嵴腔通过同一折叠轮廓通向膜间隙。移除封住外膜出口的重复后壳。

验证：实际内膜轮廓闭合，所有点位于内膜外包络以内；只有一个连接内膜组件，未保留浮动嵴条。 原有MOMP时间门控与外膜出口释放路径保留，noStress不释放。

边界：采用剖面挤出膜壁展示通路，未重建组织特异的完整三维嵴网络。

## signals-06 · fixed

每个Apaf-1分别生成七叶、八叶两个β螺旋桨，维持七Apaf平台。

验证：直接统计实际叶片mesh，七叶域共49片、八叶域共56片。

边界：叶片是结构域示意，不是原子折叠重建，caspase图标不是化学计量。

## signals-07 · fixed

母细胞和含核叶共同形成连续等值膜面，去掉始终闭合的母膜内面与过小固定圆柱颈；出核时核膜有局部腰部形变，包膜颈随重叠区自然变窄，核完全移出后形成独立排出核膜。

验证：p=0/.74/.78/.81/.835/.86/.89/.92/.95/1：采样实际核表面顶点，相对实际质膜三角面逐点射线奇偶判定均在内；初态1个、终态2个闭合膜组件。 impaired保持单一闭合膜体；网织红细胞仍保留RNA/细胞器。

边界：该路径表达小鼠出核的形变及包膜拓扑，不主张特定唯一收缩环力学机制。

## signals-08 · fixed

肽组在装载后采用与MHC相同旋转，整链保持在MHC局部槽内，运输、融合和识别全过程同步。

验证：matched和unmatched在.46/.5/.53/.56/.6/.72/.78/.81/.94/1全部将残基点逆变换到MHC局部坐标，轴向位置不变，位于槽底上及壁间。

边界：保留原TCR/CD8朝向；不宣称精确原子对接角或完整突触。

## 本组检查

- `node src/processes/modules/signals/science.test.mjs`：通过。覆盖8问题、全部条件、实际三角面/管面/局部坐标，以及可变几何字节往返跳转、有限顶点和资源稳定。
- `node src/processes/modules/signals/smoke.mjs`：四模型通过，包含双语阶段、条件、几何与浏览器目标打包。
- 仅格式化本组7个JS/MJS文件。

新增 `continuousMembrane.js` 和 `science.test.mjs`；修改 `structuralKit.js` 及四个 Process 模块。原始科学来源已在Phase A实际核查，本轮把相关RCSB/论文加入对应模型来源列表；精确列表见同名JSON。

连续膜采用解析法线优化，保留相同等值面与网格闭合条件；本地12个出芽阶段采样平均约24.1ms/凋亡更新、25.6ms/分化更新。此数值不含浏览器/GPU渲染，不是帧率保证。测试不替代视觉复核或完整科学证明。
